a. Câu lệnh để cài đặt email và name:
    git config --global user.name "Name"
    git config --global user.email "Email"


b. Setup lại name   
    git config --global user.name "PW - Ngoc Tan"

c. commit
    git commit -m"Description"

d. push code
    git push origin main
